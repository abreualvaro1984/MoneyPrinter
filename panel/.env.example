DEBUG=True
SECRET_KEY=dev-only-change-me-moneyprinter-panel
ALLOWED_HOSTS=127.0.0.1,localhost

# SQLite por padrão (já configurado). Para Postgres:
# DATABASE_URL=postgres://user:pass@localhost:5432/moneyprinter

# Google Cloud OAuth client (tipo Web) — baixe o JSON no Console.
YOUTUBE_CLIENT_SECRETS=panel/credentials/youtube_client_secret.json
YOUTUBE_OAUTH_REDIRECT_URI=http://127.0.0.1:8010/channels/oauth/callback/

# API Key do YouTube Data API v3 (pesquisa/trends). Recomendado.
YOUTUBE_API_KEY=

# Defaults PT-BR do motor Create
PANEL_DEFAULT_VOICE=pt-BR-FranciscaNeural-Female
PANEL_DEFAULT_LANGUAGE=pt-BR
PANEL_DEFAULT_ASPECT=9:16
PANEL_DEFAULT_VIDEO_SOURCE=pexels

# Opcional: detector anti-IA (GPTZero). Sem chave, usa heurística local.
GPTZERO_API_KEY=
